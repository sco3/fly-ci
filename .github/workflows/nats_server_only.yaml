name: NATS Server Smoke Test

on:
  push:
    branches:
      - main

env:
  # application name
  TEST_APP_NAME: nats-smoke-${{ github.run_id }}
  # fly org name
  FLY_ORG: ${{ secrets.FLY_ORG }}
  
jobs:  
  smoke_test: 
    runs-on: ubuntu-latest
    steps: 
      - name: Say Hello
        run: echo "Hello Workflow! This job is running correctly."